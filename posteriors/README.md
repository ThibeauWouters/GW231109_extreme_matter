# Posterior files

The `bilby` posterior files have been postprocessed into marginalized posteriors that can be stored on git for postprocessing, downstream analyses, plotting,...

## data

The posterior files during/after referee report. 

## backup

These are the posteriors before the referee review. Not stored in the git, but they are here on the Jarvis cluster: `/work/wouters/GW231109/GW231109_extreme_matter/posteriors/backup`